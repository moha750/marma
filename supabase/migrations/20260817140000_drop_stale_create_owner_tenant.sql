-- حذف النسخة القديمة من create_owner_tenant ذات الوسيط الواحد.
--
-- كيف نشأت: 20260703100409 أنشأتها بتوقيع (p_name text). ثم 20260703214945
-- أضافت الجوّال بتوقيع (p_name text, p_phone text default null). و
-- `create or replace` يستبدل التوقيع المطابق فقط — فلم تُستبدل الأولى، بل
-- وُلدت نسخة ثانية بجانبها، وبقيت القديمة على الخادم منذ ذلك الحين.
--
-- لماذا تُحذف الآن: PostgREST يحلّ الدالة بأسماء الوسائط المرسلة، فأي نداء
-- يرسل p_name وحده يصيب القديمة — فيمنح ٣ أيام بدل أسبوع، ويتخطّى شرط جوّال
-- المالك (PHONE_REQUIRED / INVALID_PHONE) كلياً. الواجهة اليوم ترسل الاثنين
-- معاً (src/shared/components/onboarding.js) فالمسار الحيّ سليم، لكن بقاء
-- النسخة ثغرةً مفتوحة بلا مستفيد.
--
-- وهي كذلك تجعل نداء SQL المباشر create_owner_tenant('x') ملتبساً بين
-- التوقيعين، لأن الثانية تقبل وسيطاً واحداً بفضل default null.

drop function if exists public.create_owner_tenant(p_name text);
